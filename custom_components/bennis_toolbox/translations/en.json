{
  "config": {
    "step": {
      "user": {
        "title": "Benni's Toolbox",
        "description": "Umbrella/overview integration for all Toolbox modules.",
        "data": { "show_missing": "Show missing modules in overview" }
      }
    },
    "abort": { "already_configured": "Benni's Toolbox is already configured." }
  },
  "options": {
    "step": {
      "init": {
        "title": "Toolbox options",
        "data": { "show_missing": "Show missing modules in overview" }
      }
    }
  }
}
